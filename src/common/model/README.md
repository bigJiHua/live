# 公共模型层 (Model)

本目录用于存放可复用的公共数据模型。

## 文件说明

当前没有启用中的公共模型文件。

## 说明

目前主要业务模型位于各模块的 `modules/xxx/model/` 目录下；如需跨模块复用，再在此目录新增明确被服务或控制器引用的模型。

---

## 更新日志

### 5月28日
- **删除** `transaction.js` — 已被各业务模块独立 Model 取代，不再需要公共事务模型。
