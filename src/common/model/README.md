# 公共模型层 (Model)

本目录包含可复用的公共数据模型。

## 文件说明

### transaction.js
**账务流水模型**

提供账务流水相关的数据操作方法。

#### 方法

| 方法 | 说明 |
|------|------|
| - | 账务流水数据操作 |

## 说明

目前主要业务模型位于各模块的 `modules/xxx/model/` 目录下，此目录用于存放跨模块复用的公共数据模型。
